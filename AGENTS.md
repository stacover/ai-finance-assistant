# 项目学习协作约定

本项目用于学习 Java Agent 应用开发，并形成可运行、可演示、可用于求职交流的真实作品。

## 开始工作时

- 先阅读 `docs/learning/PROGRESS.md`，根据当前任务查看路线和会话日志。以当前代码和测试结果核实记录，不重复从零教学。
- 路线：`docs/superpowers/plans/2026-09-16-learning-roadmap.md`。
- 会话记录：`docs/learning/SESSION_LOG.md`。

## 分工

- 用户亲手实现 Agent 核心学习任务：决策循环、停止条件、工具编排、上下文管理、证据使用、效果评估。
- 学习重心是 Agent。RAG 达到应用层理解即可：理解分块、Embedding、TopK 的作用，能接入检索工具、检查引用并处理无证据场景。向量存储与入库工程由助手完成，不要求用户深入检索算法、模型训练或向量索引内部实现。
- 助手直接完成普通实体类、DTO、枚举、Mapper、基础 CRUD、JSON 解析、空值处理、基础异常处理、配置、测试夹具、测试、文档和演示页面。
- 核心接口和数据结构先说明语义，再由助手补齐样板代码。不要把一般 Java 健壮性工作反复作为 Agent 学习作业。
- 每次核心练习给出问题背景、目标、相关文件、少量提示和验收条件；默认不直接提供完整核心实现。用户明确要求代写时，按当次要求执行。
- 用户完成后，先检查实际改动，再给出具体反馈；助手处理辅助性修补。一次推进一个核心概念。

## 记录和验收

- 每次有实质进展后更新进度看板和会话日志：实际完成内容、实现者、验证证据、用户学习证据、遗留问题、下一步。
- 项目实现和知识掌握分开记录。助手写完代码、测试通过、用户说“完成了”，都不能单独证明用户已经掌握知识。
- 学习状态只依据用户的解释、亲手实现和复盘；信息不足标为“待确认”。不推测学习时长，不虚构完成比例或评估指标。
- 区分代码存在、单元测试通过、真实服务联调通过和部署验收通过。Mock 工具和模拟模型的测试不能标为真实数据或真实模型验证。
- 保留用户未提交修改；文档更新不自动触发提交、发布或整条路线实施。

## 已确认的代码语义

- `AgentStep.batchDurationMs` 是一轮工具批次耗时；`ToolResult.durationMs` 是单个工具包含重试和退避的总耗时。
- 工具调用和返回结果按调用 ID 关联，不能按工具名称关联。
- Runtime 处理技术性重试；Agent 负责决定继续调查、选择其他工具或基于有限证据结束。
